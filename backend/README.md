# BookReads — Backend

Express + MongoDB REST API for the [BookReads](../README.md) bookstore application.

See the [root README](../README.md) for full setup instructions, environment variables,
seed scripts, and the API reference.

## Quick start

```bash
npm install
cp .env.example .env   # fill in DB_URL and JWT_SECRET_KEY
npm run start:dev
```
